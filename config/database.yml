defaults: &defaults
  adapter: postgresql
  host: localhost
  port: 5432
  username: postgres
  password: postgres
  schema_search_path: public
  encoding: utf8
  template: template1

development:
  <<: *defaults
  database: development

test:
  <<: *defaults
  database: test

production:
  <<: *defaults
  database: production